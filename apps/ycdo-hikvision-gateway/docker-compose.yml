services:
  hikvision-gateway:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "9000:9000"
    volumes:
      - ./data:/app/data
      - ./config:/app/config:ro
